## 모든 연관관계는 지연 로딩으로 하고 싶다.

- @ManyToOne , @OneToOne을 보면 fetch = LAZY로 바꾼다.

- 디폴트가 즉시로딩이기 때문에



## 영속성 전이 설정

- Order 에서 Delivery로 갈 때 라이프 사이클을 맞추겠다.

- Order를 생성할 때 Delivery도 persist하겠다.
```java
@OneToOne(~ , cascade = ALL) // static import 한것
```
- 